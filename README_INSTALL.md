# 🚀 LectureConnaissance — Frontend React

## Installation

```bash
# 1. Installer les dépendances
npm install

# 2. Configurer l'URL de l'API (optionnel, défaut: http://localhost:8000/api)
# Créer un fichier .env.local :
echo "VITE_API_URL=http://localhost:8000/api" > .env.local

# 3. Lancer le serveur de développement
npm run dev
```

## Compte admin (créé via le seeder backend)

| Email | Mot de passe |
|-------|-------------|
| admin@lectureconnaissance.com | Admin@2024! |

Après connexion, vous serez automatiquement redirigé vers `/admin`.

## Build production

```bash
npm run build
```
